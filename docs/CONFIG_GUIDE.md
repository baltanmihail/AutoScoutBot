# ⚙️ Руководство по настройкам config.py

## 📋 Содержание
1. [Настройки самообучения](#настройки-самообучения)
2. [Настройки RAG](#настройки-rag)
3. [Настройки GigaChat](#настройки-gigachat)
4. [Админ-панель](#админ-панель)

---

## 🧠 Настройки самообучения

Все настройки находятся в **`config.py`** в конце файла:

```python
# ============================================================================
# НАСТРОЙКИ САМООБУЧЕНИЯ (AI Learning)
# ============================================================================

# Автоматическое обучение (Continuous Learning)
CONTINUOUS_LEARNING = {
    "enabled": True,                    # Включить/выключить автоматическое обучение
    "queries_threshold": 20,            # Обучаться каждые N запросов
    "hours_interval": 24,               # Или каждые N часов
}

# Движок самообучения (Self Learning Engine)
SELF_LEARNING = {
    "min_samples": 3,                   # Минимум запросов для выявления паттерна
    "min_ai_relevance": 80,             # Минимальная AI relevance для успешного примера
    "max_patterns": 50,                 # Максимум паттернов для хранения
    "max_few_shot_examples": 10,       # Максимум few-shot примеров на категорию
}

# Fine-tuning
FINE_TUNING = {
    "min_examples": 100,                # Минимум примеров для экспорта
    "output_file": "finetuning_dataset.jsonl",
    "include_rag_similarity": True,     # Включать RAG similarity в датасет
}
```

---

## 🎯 Параметры по умолчанию

### CONTINUOUS_LEARNING

| Параметр | По умолчанию | Описание | Рекомендации |
|----------|--------------|----------|--------------|
| `enabled` | `True` | Включить авто-обучение | Всегда `True` |
| `queries_threshold` | `20` | Обучаться каждые N запросов | 10-30 (меньше = чаще обучается) |
| `hours_interval` | `24` | Или каждые N часов | 12-48 (резервный триггер) |

**Примеры:**

```python
# Быстрое обучение (для тестирования)
CONTINUOUS_LEARNING = {
    "enabled": True,
    "queries_threshold": 10,    # Каждые 10 запросов
    "hours_interval": 12,        # Или каждые 12 часов
}

# Медленное обучение (для стабильности)
CONTINUOUS_LEARNING = {
    "enabled": True,
    "queries_threshold": 50,    # Каждые 50 запросов
    "hours_interval": 48,        # Или каждые 2 дня
}

# Отключить авто-обучение (только вручную)
CONTINUOUS_LEARNING = {
    "enabled": False,
    "queries_threshold": 999999,
    "hours_interval": 999999,
}
```

---

### SELF_LEARNING

| Параметр | По умолчанию | Описание | Рекомендации |
|----------|--------------|----------|--------------|
| `min_samples` | `3` | Минимум повторений для паттерна | 2-5 (меньше = больше паттернов) |
| `min_ai_relevance` | `80` | Минимальная оценка успеха | 70-90 (ниже = больше примеров) |
| `max_patterns` | `50` | Лимит паттернов | 30-100 |
| `max_few_shot_examples` | `10` | Примеров на категорию | 5-15 |

**Примеры:**

```python
# Агрессивное обучение (много данных)
SELF_LEARNING = {
    "min_samples": 2,         # Мало повторений = больше паттернов
    "min_ai_relevance": 70,   # Ниже порог = больше примеров
    "max_patterns": 100,      # Больше паттернов
    "max_few_shot_examples": 15,
}

# Консервативное обучение (качество > количество)
SELF_LEARNING = {
    "min_samples": 5,         # Больше повторений = меньше, но точнее
    "min_ai_relevance": 90,   # Только отличные примеры
    "max_patterns": 30,       # Меньше паттернов
    "max_few_shot_examples": 5,
}
```

---

### FINE_TUNING

| Параметр | По умолчанию | Описание | Рекомендации |
|----------|--------------|----------|--------------|
| `min_examples` | `100` | Минимум для экспорта | 100-200 (качество датасета) |
| `output_file` | `finetuning_dataset.jsonl` | Имя файла | Без изменений |
| `include_rag_similarity` | `True` | Добавлять RAG метрики | `True` (для анализа) |

---

## 🎛️ Сценарии использования

### Сценарий 1: Новый бот (мало данных)
```python
CONTINUOUS_LEARNING = {
    "enabled": True,
    "queries_threshold": 15,    # Обучаться часто
    "hours_interval": 12,
}

SELF_LEARNING = {
    "min_samples": 2,           # Низкий порог
    "min_ai_relevance": 75,     # Принимать больше примеров
    "max_patterns": 50,
    "max_few_shot_examples": 10,
}
```

**Зачем:** Быстро накопить паттерны, система начнет учиться раньше.

---

### Сценарий 2: Зрелый бот (много данных)
```python
CONTINUOUS_LEARNING = {
    "enabled": True,
    "queries_threshold": 30,    # Обучаться реже
    "hours_interval": 24,
}

SELF_LEARNING = {
    "min_samples": 5,           # Высокий порог
    "min_ai_relevance": 85,     # Только качественные
    "max_patterns": 100,        # Больше места для паттернов
    "max_few_shot_examples": 15,
}
```

**Зачем:** Фокус на качестве, меньше "шума".

---

### Сценарий 3: Тестирование
```python
CONTINUOUS_LEARNING = {
    "enabled": True,
    "queries_threshold": 5,     # Очень часто
    "hours_interval": 1,
}

SELF_LEARNING = {
    "min_samples": 1,           # Любой пример
    "min_ai_relevance": 50,     # Низкий порог
    "max_patterns": 200,
    "max_few_shot_examples": 20,
}
```

**Зачем:** Быстро проверить, как работает самообучение.

---

## 📊 Админ-панель - Новые функции

### Доступ
```
Telegram бот → Меню → Админ-панель
```

### Что добавлено

#### 1. **Статистика RAG**
```
🔍 RAG-система:
• Проиндексировано: 5153 стартапов
• Метод: GigaChat Embeddings
```

#### 2. **Статистика поиска**
```
📊 Система поиска:
• Всего запросов: 87
• Средняя AI relevance: 76.3/100
• Хороших результатов (≥80): 45
• Качество: 🟡 Хорошо
```

**Индикаторы качества:**
- 🟢 ≥ 80 = Отлично
- 🟡 60-80 = Хорошо
- 🔴 < 60 = Требует улучшения

#### 3. **Самообучение**
```
🧠 Самообучение:
• Выявлено паттернов: 12
• Few-shot примеры: ✅ Созданы
• Авто-обучение: ✅ Активно
• Запросов до обучения: 7/20
```

#### 4. **Fine-tuning**
```
🚀 Fine-tuning:
• Доступен! (145 примеров)
• Запустите: python ai_learning/train_model.py
```

или

```
🚀 Fine-tuning:
• Прогресс: 67/100 примеров
```

---

### Новые кнопки

#### 🧠 Статистика самообучения
**Показывает:**
- Топ-5 паттернов с деталями
- Качество поиска (avg/min/max AI relevance)
- Топ-5 кластеров
- Статус continuous learning
- Последнее обучение

**Пример вывода:**
```
🧠 СТАТИСТИКА САМООБУЧЕНИЯ

📊 Паттерны:
• Всего выявлено: 12

🔥 Топ-5 паттернов:
1. clean_tech
   Слова: переработка, пластик, экология...
   Кластер: Энерготех
   Успех: 92% (15 использований)

2. ai_ml
   Слова: искусственный интеллект, машинное...
   Кластер: ИТ
   Успех: 88% (12 использований)

📈 Качество поиска:
• Всего запросов: 87
• Средняя AI relevance: 76.3/100
• Диапазон: 10 - 95

🎯 Топ-5 кластеров:
1. ИТ: 35 результатов (AI=72)
2. Энерготех: 28 результатов (AI=80)
...

⚙️ Авто-обучение:
• Статус: ✅ Активно
• Запросов до обучения: 7/20
• Последнее обучение: 03.12.2025 21:45
```

---

#### 🎓 Запустить обучение
**Что делает:**
1. Анализирует все данные в query_history.db
2. Выявляет новые паттерны
3. Создает few-shot примеры
4. Генерирует синонимы
5. Экспортирует для fine-tuning (если ≥100 примеров)

**Пример вывода:**
```
✅ ОБУЧЕНИЕ ЗАВЕРШЕНО

📊 Результаты:
• Выявлено паттернов: 3
• Обновлено паттернов: 5
• Сгенерировано синонимов: 12
• Создано few-shot примеров: 2

💡 Рекомендации:
• ✅ Выявлено 12 паттернов. Система учится эффективно!
• 🚀 Достаточно данных для fine-tuning!

🚀 Fine-tuning:
✅ Экспортировано 145 примеров
Файл: finetuning_dataset.jsonl
```

---

## 🔄 Как изменения применяются

### Изменения в config.py
```bash
# 1. Откройте config.py
notepad config.py

# 2. Измените параметры
CONTINUOUS_LEARNING = {
    "queries_threshold": 10,  # Было 20
}

# 3. Сохраните файл

# 4. Перезапустите бота
python bot.py
```

**Важно:** Изменения применяются только после перезапуска бота!

---

## 📝 Мониторинг настроек

### Через админ-панель
```
Админка → 🧠 Статистика самообучения
```

Смотрите:
- Запросов до обучения: 7/20 ← queries_threshold
- Паттернов: 12 ← зависит от min_samples
- Средняя AI: 76.3 ← зависит от min_ai_relevance

### Через логи
```bash
notepad terminals\11.txt
```

Ищите:
```
✅ Continuous learning запущен
🧠 Накоплено 20 запросов. Запуск обучения...
📊 Обучение завершено: паттернов=3, примеров=2
```

### Через query_history.db
```sql
SELECT COUNT(*) FROM query_patterns;
-- Количество паттернов

SELECT AVG(success_rate) FROM query_patterns;
-- Средняя успешность паттернов
```

---

## 💡 Рекомендации

### Как выбрать queries_threshold?

```
Мало запросов в день (< 50):
→ queries_threshold = 10-15

Среднее (50-200):
→ queries_threshold = 20 (по умолчанию)

Много (> 200):
→ queries_threshold = 30-50
```

### Как выбрать min_samples?

```
Разнообразные запросы:
→ min_samples = 2-3 (больше паттернов)

Повторяющиеся запросы:
→ min_samples = 5-7 (меньше, но точнее)
```

### Как выбрать min_ai_relevance?

```
Мало данных:
→ min_ai_relevance = 70-75 (принимать больше)

Много данных:
→ min_ai_relevance = 85-90 (только лучшие)
```

---

## 🎉 Итоги

### ✅ Что настроено
- Автоматическое обучение (каждые N запросов или часов)
- Пороги для паттернов и примеров
- Экспорт для fine-tuning
- Админ-панель с детальной статистикой

### 🎯 Рекомендуемые настройки
```python
# Для большинства случаев:
CONTINUOUS_LEARNING = {
    "enabled": True,
    "queries_threshold": 20,
    "hours_interval": 24,
}

SELF_LEARNING = {
    "min_samples": 3,
    "min_ai_relevance": 80,
    "max_patterns": 50,
    "max_few_shot_examples": 10,
}
```

### 📚 Дополнительно
- **docs/ai_learning.md** - Подробно о самообучении
- **docs/START_HERE.md** - Общий обзор системы
- **docs/FULL_EXPLANATION.md** - Как все работает

**Вопросы?** Проверьте админ-панель - там вся статистика! 📊✨

