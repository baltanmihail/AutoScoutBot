# 📊 Мониторинг и Аналитика AutoScoutBot

## 🚀 Быстрый доступ

### Основные команды

| Команда | Что показывает | Время |
|---------|----------------|-------|
| `python view_stats.py` | Общая статистика | 10 сек |
| `python analyze_query.py` | Список запросов | 5 сек |
| `python analyze_query.py 23` | Детали запроса #23 | 5 сек |
| `notepad terminals\11.txt` | Логи бота | мгновенно |

---

## 📁 Структура проекта (новые файлы)

```
AutoScoutBot/
├── services/
│   ├── reranker.py              # 🎯 Re-ranking через GigaChat
│   ├── query_history.py         # 💾 История запросов (БД)
│   ├── few_shot_examples.py     # 🧠 Примеры для обучения
│   └── rag_service.py           # 🔍 Семантический поиск
│
├── query_history.db             # 💾 База данных (создается автоматически)
│
├── view_stats.py                # 📊 Просмотр статистики
├── analyze_query.py             # 🔍 Анализ запроса
│
├── docs/                        # 📚 Вся документация
│   ├── START_HERE.md
│   ├── FULL_EXPLANATION.md
│   ├── README_MONITORING.md     # 📊 Этот файл
│   └── CONFIG_GUIDE.md
```

---

## 📊 Что показывает view_stats.py

### Пример вывода:

```
============================================================
📊 СТАТИСТИКА AUTOSCOUTBOT
============================================================
Всего запросов: 45
Всего результатов: 135
Средняя AI релевантность: 73.24/100

🔥 ТОП-5 КЛАСТЕРОВ:
  • ИТ: 45 результатов
  • Энерготех: 35 результатов
  • Биомедицина: 25 результатов
  • Промтех: 18 результатов
  • Агротех: 12 результатов

📝 ПОСЛЕДНИЕ 10 ЗАПРОСОВ:
  [ 45] переработка пластика, clean tech               (pro, 2025-12-03 21:4)
  [ 44] AI в медицине, диагностика                     (max, 2025-12-03 21:3)
  [ 43] дроны для энергетики                           (pro, 2025-12-03 21:1)
  ...

⭐ ТОП-10 ЛУЧШИХ СОВПАДЕНИЙ (AI ≥ 80):
  • ЭКОПЛАСТИК                    → "переработка пластика..." (AI=95, Энерготех)
  • БИОМАТЕРИАЛЫ                  → "переработка пластика..." (AI=92, Энерготех)
  • МЕДТЕХ АИ                     → "AI в медицине..." (AI=88, Биомедицина)
  ...

⚠️ ХУДШИЕ СОВПАДЕНИЯ (AI < 50) - Нужно улучшить:
  • ИТ РЕШЕНИЯ                    → "переработка пластика..." (AI=10, ИТ)
  • КИБЕРСПОРТ                    → "банковский сектор..." (AI=15, ИТ)
  ...
============================================================
```

### Интерпретация:

#### Средняя AI релевантность:
- **80-100:** 🟢 Отлично! Система работает идеально
- **60-79:** 🟡 Хорошо, есть что улучшить
- **40-59:** 🟠 Средне, нужны few-shot примеры
- **< 40:** 🔴 Плохо, срочно нужны улучшения

#### Топ кластеры:
Показывает, в каких кластерах чаще всего находятся результаты.  
Полезно для понимания, какие отрасли интересуют пользователей.

#### Лучшие совпадения (AI ≥ 80):
Примеры успешных результатов. Система хорошо справилась.

#### Худшие совпадения (AI < 50):
Ошибки системы. Нужно проанализировать и добавить примеры.

---

## 🔍 Что показывает analyze_query.py

### Без параметров:
```bash
python analyze_query.py
```

Показывает список последних 20 запросов для выбора.

---

### С ID запроса:
```bash
python analyze_query.py 45
```

### Пример вывода:

```
======================================================================
🔍 АНАЛИЗ ЗАПРОСА #45
======================================================================
📝 Запрос: переработка пластика, clean tech
🎯 Модель: pro
🔄 Расширенный: переработка пластика, полимеры, биоразлагаемые материалы...
⏰ Время: 2025-12-03 21:43:15
🔧 Фильтры: {"status": "active", "max_profit": 20000000}

📊 РЕЗУЛЬТАТЫ:
Поз  Название                  Кластер         RAG      AI       Δ
----------------------------------------------------------------------
1    ЭКОПЛАСТИК                Энерготех       89.2%    95.0%  📈   +6
2    БИОМАТЕРИАЛЫ              Энерготех       86.5%    92.0%  📈   +5
3    ПЕРЕРАБОТКА               Энерготех       87.0%    55.0%  📉  -32

📈 МЕТРИКИ:
  • Средняя AI релевантность: 80.7/100
  • Минимум: 55/100
  • Максимум: 95/100
  ✅ Отличные результаты!
======================================================================
```

### Интерпретация:

#### Δ (Дельта):
- **📈 Положительная (+6, +5):** Re-ranking улучшил оценку, нашел скрытую релевантность
- **📉 Отрицательная (-32):** Re-ranking исправил ошибку RAG
  - В примере: "ПЕРЕРАБОТКА" = переработка древесины (не пластика!)
  - RAG: 87% (похожий текст)
  - AI: 55% (другая область) ← Правильно!

#### Средняя AI релевантность:
- Показывает общее качество результатов для этого запроса
- Если < 50 → Запрос слишком общий или нет подходящих стартапов

---

## 📝 Логи (terminals\11.txt)

### Как открыть:

#### Вариант 1: Блокнот (статично)
```bash
notepad terminals\11.txt
```

#### Вариант 2: PowerShell (в реальном времени)
```bash
Get-Content terminals\11.txt -Wait -Tail 50
```

---

### Что искать в логах:

#### 1. Начало поиска:
```
🔍 Используется RAG-поиск с улучшениями (Re-ranking + Few-shot)
```

#### 2. Результаты RAG:
```
✅ Семантический поиск: найдено 300 кандидатов из 5153 стартапов
```
**Норма:** 100-500 кандидатов  
**Мало (<50):** Слишком узкий запрос  
**Много (>800):** Слишком общий запрос

---

#### 3. Re-ranking (ВАЖНО!):
```
🔄 Re-ranking: оценка 30 кандидатов через GigaChat
  1. ЭКОПЛАСТИК: RAG=0.892, AI=95
  2. БИОМАТЕРИАЛЫ: RAG=0.865, AI=92
  3. ПЕРЕРАБОТКА: RAG=0.870, AI=55  ← Смотрим сюда!
✅ Re-ranking завершен: топ-10 выбраны
```

**Как читать:**
- Сравниваем RAG и AI
- RAG=0.870 (87%) → AI=55: **Большая разница!** Re-ranking исправил ошибку
- RAG=0.892 (89%) → AI=95: **Маленькая разница** (+6%), RAG был почти прав

---

#### 4. Few-shot примеры:
```
✅ Few-shot примеры добавлены в промпт
```
**Если НЕТ этой строки:** Few-shot не сработал (категория не найдена)

---

#### 5. Сохранение:
```
💾 Запрос сохранен в историю: ID=45
💾 Сохранено 3 результатов для query_id=45
```

---

#### 6. Итог:
```
✅ Итого: выбрано 3 стартапов из 10 топ-кандидатов
```

---

### Типичные проблемы в логах:

#### Проблема 1: Re-ranking не запускается
```
✅ Семантический поиск: найдено 300 кандидатов
✅ Итого: выбрано 3 стартапов  ← НЕТ строки "Re-ranking"!
```
**Причина:** GigaChat недоступен или мало кандидатов (<3)  
**Решение:** Проверить токен GigaChat, лимиты API

---

#### Проблема 2: Few-shot не работает
```
✅ Re-ranking завершен
✅ Итого: выбрано 3  ← НЕТ строки "Few-shot"!
```
**Причина:** Категория не найдена в `few_shot_examples.py`  
**Решение:** Добавить примеры для этой категории

---

#### Проблема 3: Ошибка сохранения
```
⚠️ Ошибка сохранения в историю: [текст ошибки]
```
**Причина:** Проблема с БД  
**Решение:** Проверить, существует ли `query_history.db`, права доступа

---

## 💾 База данных (query_history.db)

### Структура:

```sql
-- Таблица запросов
queries:
  - id (автоинкремент)
  - user_id
  - query_text (текст запроса)
  - model_type (standard/pro/max)
  - timestamp
  - expanded_query (расширенный запрос от GigaChat)
  - filters_used (JSON с фильтрами)

-- Таблица результатов
query_results:
  - id
  - query_id (связь с queries)
  - startup_name
  - startup_id
  - rag_similarity (0.0 - 1.0)
  - ai_relevance (0 - 100)
  - position (1, 2, 3)
  - cluster
  - technologies

-- Таблица паттернов (для адаптивного обучения)
query_patterns:
  - id
  - query_type
  - keywords
  - relevant_clusters
  - example_query
  - example_startups
  - success_rate (0.0 - 1.0)
```

---

### Просмотр через SQLite Browser:

1. Скачать: https://sqlitebrowser.org/
2. Открыть `query_history.db`
3. Вкладка "Browse Data" → выбрать таблицу

---

### SQL запросы для анализа:

```sql
-- Топ-10 запросов по средней релевантности
SELECT 
    q.query_text,
    AVG(r.ai_relevance) as avg_relevance,
    COUNT(*) as result_count
FROM queries q
JOIN query_results r ON q.id = r.query_id
WHERE r.ai_relevance > 0
GROUP BY q.id
ORDER BY avg_relevance DESC
LIMIT 10;

-- Какие кластеры чаще всего релевантны для "AI"
SELECT 
    r.cluster,
    AVG(r.ai_relevance) as avg_relevance,
    COUNT(*) as count
FROM queries q
JOIN query_results r ON q.id = r.query_id
WHERE q.query_text LIKE '%AI%'
GROUP BY r.cluster
ORDER BY avg_relevance DESC;

-- Динамика релевантности по времени
SELECT 
    DATE(q.timestamp) as date,
    AVG(r.ai_relevance) as avg_relevance,
    COUNT(DISTINCT q.id) as query_count
FROM queries q
JOIN query_results r ON q.id = r.query_id
WHERE r.ai_relevance > 0
GROUP BY DATE(q.timestamp)
ORDER BY date;
```

---

## 🎯 Практические кейсы

### Кейс 1: Новая категория запросов

**Ситуация:** Пользователи начали спрашивать про "квантовые компьютеры"

**Действия:**
1. Проверить статистику:
   ```bash
   python view_stats.py
   ```
   
2. Найти запросы с "квантовыми":
   ```bash
   python analyze_query.py  # Смотрим ID
   python analyze_query.py 47
   ```

3. Если AI релевантность < 60:
   - Открыть `services/few_shot_examples.py`
   - Добавить категорию "quantum":
     ```python
     "quantum": {
         "description": "Квантовые технологии",
         "examples": [{
             "query": "квантовые компьютеры",
             "relevant": ["Квантовые процессоры", ...],
             "clusters": ["ИТ", "Промтех"],
             "keywords": ["квант", "кубит", "суперпозиция"]
         }]
     }
     ```
   - Перезапустить бота

---

### Кейс 2: Re-ranking работает плохо

**Симптомы:**
```bash
python view_stats.py
# Средняя AI релевантность: 35/100 ❌
```

**Диагностика:**
1. Проверить логи:
   ```bash
   notepad terminals\11.txt
   ```
   Ищем: "Re-ranking"

2. Если НЕТ строки "Re-ranking":
   - GigaChat недоступен
   - Проверить токен, лимиты

3. Если ЕСТЬ, но оценки странные:
   - Проверить конкретные запросы:
     ```bash
     python analyze_query.py 48
     ```
   - Добавить few-shot примеры

---

### Кейс 3: Система не обучается

**Симптомы:**
- Запросов много (50+), но качество не растет

**Причины:**
1. Few-shot примеры не добавляются
2. Паттерны не создаются

**Диагностика:**
```python
# Проверить паттерны
import sqlite3
conn = sqlite3.connect("query_history.db")
cursor = conn.cursor()

cursor.execute("SELECT COUNT(*) FROM query_patterns")
print(f"Паттернов: {cursor.fetchone()[0]}")

# Если 0 → Проблема!
```

**Решение:** Вручную добавить паттерны через `query_history.update_pattern(...)`

---

## 📈 KPI для мониторинга

### Каждую неделю проверяйте:

| Метрика | Целевое значение | Критическое значение |
|---------|------------------|---------------------|
| Средняя AI релевантность | > 70 | < 40 |
| % результатов с AI ≥ 80 | > 30% | < 10% |
| Количество запросов | Растет | Падает |
| Топ кластеры | Разнообразие | Только 1-2 |

---

## 🔔 Алерты (когда бить тревогу)

### 🔴 Критично:
- Средняя AI < 30
- Re-ranking не работает 3+ дня
- БД не сохраняется

### 🟡 Внимание:
- Средняя AI 40-60
- Мало разнообразия кластеров
- Много худших совпадений (AI < 30)

### 🟢 Норма:
- Средняя AI > 70
- Re-ranking работает
- Few-shot примеры добавляются

---

## 📚 Дополнительная документация

- **docs/START_HERE.md** - С чего начать
- **docs/FULL_EXPLANATION.md** - Подробное объяснение (читать обязательно!)
- **docs/CONFIG_GUIDE.md** - Настройки и fine-tuning

---

## 💬 Поддержка

**Вопросы по мониторингу?**  
Все ответы в `HOW_IT_WORKS.md` - там 100+ примеров с объяснениями!

